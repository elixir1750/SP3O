# Required paths
HF_CHECKPOINT=/path/to/Qwen3-4B-Base
MEGATRON_CHECKPOINT=/path/to/Qwen3-4B-Base-megatron
MEGATRON_LM=/path/to/Megatron-LM
PROMPT_DATA=/path/to/dapo-math-17k.jsonl
OUTPUT_DIR=/path/to/experiments

# Experiment selection
PRESET=sp3o
MODEL_SIZE=4B
SEED=1234
# NUM_CRITIC_ONLY_STEPS=20

# Optional evaluation and tracking
# EVAL_CONFIG=/path/to/eval.yaml
# WANDB_API_KEY=your-key
# WANDB_MODE=offline
